from lexer_dsl import lexer
from parser import parser
from interpreter import Interpreter

# Code to interact with your DSL, for example, reading a DSL script file,
# tokenizing and parsing it, and then executing it using the interpreter.
